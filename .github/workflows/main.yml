name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
    - name: Run backend tests
      run: docker-compose run backend sh -c 'pytest'
    - name: Run frontend tests
      run: docker-compose run frontend sh -c 'yarn test'
